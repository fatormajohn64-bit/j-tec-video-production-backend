services:
  - type: web
    name: j-tec-video-production-api
    runtime: python

    buildCommand: |
      pip install -r requirements.txt
      playwright install chromium
      playwright install-deps chromium

    startCommand: |
      uvicorn app.main:app --host 0.0.0.0 --port $PORT
